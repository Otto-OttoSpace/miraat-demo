name: Arabic / RTL audit
on: [pull_request]

permissions:
  contents: read
  pull-requests: write

jobs:
  miraat:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Otto-OttoSpace/miraat-action@v1
        with:
          paths: "src"
          fail-on: "error"
